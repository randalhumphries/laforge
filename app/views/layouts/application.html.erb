<!DOCTYPE html>
<html>
  <head>
    <title>Laforge</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
  </head>

  <body>
    <div class="container wrapper">
      <% if user_signed_in? %>
        <%= render "shared/navbar" %>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
